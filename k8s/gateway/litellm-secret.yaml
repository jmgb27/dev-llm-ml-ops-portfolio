# Replace values before production. Prefer ExternalSecrets or SealedSecrets in real deployments.
apiVersion: v1
kind: Secret
metadata:
  name: litellm-credentials
  namespace: llm-gateway
  labels:
    app: litellm
type: Opaque
stringData:
  master-key: "sk-1234"
