apiVersion: v1
kind: ResourceQuota
metadata:
  name: llm-gateway-quota
  namespace: llm-gateway
spec:
  hard:
    requests.cpu: "16"
    requests.memory: 32Gi
    limits.cpu: "20"
    limits.memory: 40Gi
    pods: "30"
