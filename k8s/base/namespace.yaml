apiVersion: v1
kind: Namespace
metadata:
  name: llm-gateway
  labels:
    # Enroll namespace in Istio Ambient mesh (install Istio with profile=ambient first).
    istio.io/dataplane-mode: ambient
    app.kubernetes.io/part-of: llm-gateway
