# Local env overrides
.env

# Secrets with real credentials (use .example templates + scripts/apply-*-secret.sh)
k8s/gateway/cloudflared-secret.yaml
ansible/group_vars/secrets.yml
ansible/.cache/

# Python virtual environments
chat-ui/.venv/

# Large model weights (download separately)
models/*.gguf
