# Copy to secrets.yml (gitignored) — never commit real values.
#
#   cp ansible/group_vars/secrets.yml.example ansible/group_vars/secrets.yml
#
# Or set REGISTRY in the repo-root .env when using scripts/apply-registry-secret.sh.

container_registry: registry.example.com
